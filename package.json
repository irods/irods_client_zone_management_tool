{
  "name": "irods_client_zone_management_tool",
  "version": "0.4.1",
  "private": true,
  "dependencies": {
    "-": "^0.0.1",
    "axios": "^1.10.0",
    "cors": "^2.8.5",
    "install": "^0.13.0",
    "npm": "^11.4.2",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "react-router": "^7.6.2",
    "react-scripts": "5.0.1",
    "react-timeago": "^8.2.0",
    "sql-formatter": "^15.6.5"
  },
  "scripts": {
    "start": "export REACT_APP_VERSION=$(npm version | sed -rn 's/^ +irods_client_zone_management_tool: .(.*).,$/\\1/p') REACT_APP_GIT_SHA=$(git rev-parse HEAD) CHOKIDAR_USEPOLLING=true && react-scripts start",
    "build": "export REACT_APP_VERSION=$(npm version) REACT_APP_GIT_SHA=$(git rev-parse HEAD) && react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
